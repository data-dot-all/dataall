import PropTypes from 'prop-types';

export const MetadataFormPreview = (props) => {
  return <span>MetadataFormPreview</span>;
};

MetadataFormPreview.propTypes = {
  metadataForm: PropTypes.any.isRequired
};
