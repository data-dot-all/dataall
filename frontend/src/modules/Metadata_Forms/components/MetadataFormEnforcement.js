import PropTypes from 'prop-types';

export const MetadataFormEnforcement = (props) => {
  return <span>MetadataFormEnforcement</span>;
};

MetadataFormEnforcement.propTypes = {
  metadataForm: PropTypes.any.isRequired
};
